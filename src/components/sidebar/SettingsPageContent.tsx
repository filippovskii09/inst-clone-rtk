import SidebarBackToButton from '../shared/SidebarBackToButton';

const SettingsPageContent = () => {
  return (
    <>
      <SidebarBackToButton />
      <h5 className="w-full flex items-center justify-center">Settings</h5>
    </>
  );
};

export default SettingsPageContent;
